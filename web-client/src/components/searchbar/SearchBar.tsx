import * as React from 'react';
import './SearchBar.css';

type Props = {};

const SearchBar = (props: Props) => {
  return <input className="search-bar" placeholder="Etsi kanavia"></input>;
};

export default SearchBar;
