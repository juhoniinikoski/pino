/* eslint-disable no-unused-expressions */

const mockProps = {
  followedBy: 5,
  channelId: 'kauppikseen1234',
  followedByUser: true,
};

describe('rendering tests', () => {
  test.todo('should render a follower count');
  test.todo('should be colored by if its followed by user');
});
